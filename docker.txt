--------------------PROD---------------------

# Сборка образа для prod
docker build -t  mist3s/leaf-flow-admin-nginx:prod .

# Пуш образа в Docker Hub
docker push mist3s/leaf-flow-admin-nginx:prod

----------------------------------------------
--------------------STAGE---------------------

# Сборка образа для prod
docker build -t  mist3s/leaf-flow-admin-nginx:stage .

# Пуш образа в Docker Hub
docker push mist3s/leaf-flow-admin-nginx:stage

----------------------------------------------